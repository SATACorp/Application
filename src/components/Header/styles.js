import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  container: {},

  img: {
    width:20,
    paddingRight: 20
  }, 

  username: {
    position: 'right',
    paddingLeft: 1085
  }
}));
